# EVIDENCE cross-manuscript overlap firewall

Status: submission-production rule under the two-paper architecture.

## Active manuscript

Only the following manuscript is active for the Evidence submission path:

- `zuizui0223/ced/manuscript/EVIDENCE_DRAFT_V1.md`

The following are source/provenance documents, not concurrent independent submission targets:

- Boundary Paper A / Perspective materials;
- MROD standalone MEE manuscript and submission package;
- CED standalone Paper B framing.

## Claim ownership inside Evidence

### Boundary source

May supply:

- current observational equivalence / identification boundary;
- `k-rank(M)` structural residual dimension under the declared log-linear family;
- new identification direction versus precision distinction;
- calibration/transport boundary as supporting material.

Do not preserve Boundary as a separate paper identity inside the abstract. In Evidence it is Part I: the current structural starting point.

### MROD source

May supply:

- admissible mechanism region;
- mechanism-learning information value;
- sequential recomputation after outcomes;
- G2 truth-peek-free controlled benchmark.

Do not claim that mechanism information is the universal Evidence objective. In Evidence it is one declared utility branch.

### CED source

May supply:

- experiment-induced compatible-world quotient;
- target-safe quotient / required target-relevant refinement;
- failure-aware trustworthy refinement;
- exact/risk-limited reporting boundary;
- target-safe adaptive stopping under cost/error contracts.

Do not claim generic partition refinement as standalone novelty.

## New integrated claim

Evidence owns one new cross-source synthesis result:

> mechanism-learning information and target-licensing value can rank the same equal-cost candidate observations differently.

Canonical executable source:

- `ced/learning_licensing.py`;
- `tests/test_learning_licensing.py`;
- `manuscript/evidence_learning_licensing_result.json`.

This result is the reason MROD and CED belong in one Evidence paper rather than competing as two next-observation manuscripts.

## Observation-paper firewall

Evidence may receive a retained compatible-world set from Observation. It may not re-claim as novelty:

- generic retained-side-information refinement;
- shadow/no-row support deletion;
- semantic-coarsening loss;
- deterministic downstream irreversibility after observation-stage collapse.

Use one interface sentence:

> Observation determines which distinctions survive the recording system; Evidence determines what those surviving distinctions identify or license and what additional measurement is warranted.

## Abstract firewall

The Evidence abstract must be written around:

1. current identification boundary;
2. target-safe requirement;
3. learning-versus-licensing divergence;
4. reliability architecture;
5. objective-relative acquisition/reporting.

Do not reuse the Boundary abstract, MROD abstract or CED Paper B abstract verbatim or as sequential mini-abstracts.

## Submission gate

Evidence is not ready for upload until:

- [ ] `EVIDENCE_DRAFT_V1.md` is migrated into the canonical LaTeX production path;
- [ ] the learning-versus-licensing benchmark is included in CI and Figure 2;
- [ ] all Boundary/MROD/CED source results are pinned by commit/blob/result hashes;
- [ ] merged bibliography contains original literature rather than source-manuscript self-citations as evidence;
- [ ] archived standalone abstracts are overlap-audited;
- [ ] MEE submission package text is rewritten from standalone CED Paper B to integrated Evidence;
- [ ] reviewer archive includes pinned source snapshots without presenting them as separate submitted manuscripts.
